auth:
  jwt:
    secret: ${JWT_SECRET:}
spring:
  quartz:
    scheduler-name: quartz_
    jdbc:
      initialize-schema: always
    job-store-type: jdbc
  batch:
    jdbc:
      initialize-schema: always
      table-prefix: BATCH_
    job:
      enabled: false
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    username: ${DB_USERNAME:}
    password: ${DB_PASSWORD:}
    url: ${DB_URL:}
  jackson:
    property-naming-strategy: SNAKE_CASE
  jpa:
    generate-ddl: false
    show-sql: false
    properties:
      hibernate.order_inserts: true
      hibernate.order_updates: true
  elasticsearch:
    rest:
      username: ${ELASTIC_USERNAME}
      password: ${ELASTIC_PASSWORD}
      uris: ${ELASTIC_URL}
openapi:
  secret: H737J9D1dQ%2FX5iNhLLRIB58b3X7oDZh5daWqcIw2yF8U75pqSztkOygiAmWnguKJBCoQylxgNcYXEEkOUif3mA%3D%3D
  kkyeongki:
    secret: 1524deabed384334ba9d4bc44fe34066